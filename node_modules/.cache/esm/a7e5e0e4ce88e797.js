let createStore;_ce8‍.x([["increment",()=>increment],["decrement",()=>decrement],["default",()=>_ce8‍.o]]);_ce8‍.w("redux",[["createStore",["createStore"],function(v){createStore=v}]]);

const INCREMENT = "INCREMENT";
const DECREMENT = "DECREMENT";

//ACTION CREATORS
       const increment = (value = 1) => {
  return {
    //obje
    type: INCREMENT,
    payload: value,
  };
};

       const decrement = (value = 1) => ({
  type: DECREMENT,
  payload: value,
});

//REDUCER 
const counterReducer = (counter = 0, action) => {
  switch (action.type) {
    case INCREMENT:
      return counter + action.payload;
    case DECREMENT:
      return counter - action.payload;
    default:
      return counter;
  }
};


_ce8‍.d(counterReducer);